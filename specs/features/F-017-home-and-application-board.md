# F-017: 今日工作台与独立投递看板

## Goal

把当前 `/applications` 的混合页面拆为两个明确页面：`/` 是帮助用户决定下一步的今日工作台；`/applications` 是管理全部投递记录的独立投递看板。

## User scenarios

1. 用户打开应用先看到今日重点、正在推进的机会和资料入口，而非完整的投递管理表格。
2. 用户可在首页的“正在推进的机会”中查看至多三条真实记录，并通过“查看全部投递”进入投递看板。
3. 用户在投递看板中继续使用现有搜索、筛选、列表/看板、创建、编辑、删除和拖拽状态变更能力。

## In scope

- 新建根路由工作台首页，使用真实投递数据展示今日重点、三条推进机会和资料入口。~~后两项已被 F-045 删除（理由见"取代关系"）。~~
- `/applications` 保留为独立投递看板，移除首页概览，保留完整管理能力。
- 严格复用 `visual-lab` 的组件布局与 `visual-layered` 的字体、间距、按钮、侧栏和表格视觉 token。

## Out of scope

- 不新增、删除或修改 API、数据库模型、投递状态流转和 AI 功能。
- 不实现岗位库、面试准备或外部文档同步。

## Acceptance criteria

1. 根路由不再跳转，展示首页；首页的“查看全部投递”可进入 `/applications`。
2. ~~首页推进机会表格为公司/岗位、当前阶段、下一步、渠道四列，只显示真实的前三条推进记录。~~ 已被 F-045 取代（见下方"取代关系"）。
3. `/applications` 保留现有全部投递管理交互，并以表格为默认主视图。
4. 工作台壳层与页面字体、间距、导航选中态、主按钮颜色、卡片边界同参考基线一致，不出现 Element Plus 默认紫色主按钮或蓝色侧栏强调线。
5. `vue-tsc --noEmit` 通过。
6. 首页和投递看板复用公司/岗位、当前阶段、下一步、渠道的表格信息顺序；投递状态具有一致且可区分的语义颜色。

## Affected contracts

- 无：仅重组既有前端页面，继续调用既有投递 API。

## Risks and open questions

- 首页数据随现有筛选 API 的返回量变化；首页独立请求全部记录，不受投递看板筛选影响。

## 取代关系

F-045（今日工作台数据化重构）取代本特性中关于首页展示内容的决定：

- **「正在推进的机会」表格被删除。** 该表格与首页的「行动轨迹」卡片使用的是同一个 `activeApplications` 变量、同一个 `.slice(0, 3)`，属于同一份数据渲染两遍。两者一并移除后，首页保留一个按紧急度自动排序的「今日重点」列表。
- **「资料入口」卡片组被删除。** 简历版本、网申档案、面试准备三个入口与全局侧边栏的「准备资料」分组完全重复。
- **首页数据获取方式改变。** 本特性风险栏提到"首页独立请求全部记录"，该请求受 `pageSize` 上限 100 截断（实际 110 条），并让统计卡片显示错误数字。F-045 改为单个聚合接口返回，不再拉取投递快照。
- 本特性中与投递看板（`/applications`）相关的验收标准**仍然有效**：投递看板的全部管理交互、表格信息顺序与状态语义颜色均未改动。
