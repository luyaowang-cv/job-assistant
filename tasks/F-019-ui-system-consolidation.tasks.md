# F-019 Tasks

- [x] T-001: 建立全局 UI token 与 Element Plus 控件状态规范。  
  Verify: 输入、选择、按钮与表格采用统一尺寸、底色和焦点状态。
- [x] T-002: 收敛首页和投递看板的文本、卡片与导航密度。  
  Verify: 首页信息架构不变，辅助文字可读且卡片层级一致。
- [x] T-003: 重组个人档案和网申档案的表面层级与表单操作。  
  Verify: 个人档案为单一主表面，教育经历为内嵌区段，现有保存/新增/删除可用。
- [x] T-004: 对齐简历版本与 API 设置页面。  
  Verify: 编辑器、设置卡、说明文字和操作按钮符合统一规则。
- [x] T-005: 完成桌面/窄屏与静态检查。 
  Verify: 目标页面经桌面渲染检查；`vue-tsc --noEmit` 通过。完整 lint 被两处既有服务层错误阻断：`application-profile.service.ts` 的 type-import/未使用变量，以及 `personal-profile.service.ts` 的 type-import。
- [x] T-006: 回撤跨页面 UI 覆盖，仅保留档案页控件几何对齐并恢复投递表头层级。
  Verify: 网申档案与个人档案的输入、选择和数字控件实测均为 40px；双列控件共享固定左边界，全宽字段与左列输入起点一致；投递表头恢复可辨识背景、深色文字与 600 字重；`vue-tsc --noEmit` 通过。
