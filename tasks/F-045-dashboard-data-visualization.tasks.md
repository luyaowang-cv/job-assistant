# F-045 Tasks

- [x] T-001: 新建 `server/services/dashboard.service.ts`，实现漏斗、节奏、每日、待跟进四组聚合 ? Verify: 先用 SQL 手写口径校验，Prisma 实现后两边数值完全一致（110 / 62 / 64 / 5 / 3 / 0）
- [x] T-002: 实现 `reachedAtLeast()`：当前状态命中 **或** 历史事件中出现过进入该状态 ? Verify: 「进过笔试但后来被拒」的记录被计入，漏斗各档不小于按当前状态统计的结果
- [x] T-003: 实现 `appliedSince()`：同时统计状态变更事件与直接以已投递状态建单的记录 ? Verify: 变更 + 建单两条路径都不漏算
- [x] T-004: 新建 `GET /api/v1/dashboard/summary`，解析前端传入的 `dayStart` / `weekStart` ? Verify: 缺参时按服务端本地时间兜底，不报错
- [x] T-005: 首页改为只调用聚合接口，删除原先的 100 条投递快照请求 ? Verify: 网络面板只有一次 `/api/v1/dashboard/summary` 请求
- [x] T-006: 修正概览卡错误数字（原复用 `.slice(0,3)` 与 `pageSize` 上限） ? Verify: 「正在跟进」显示 62 而非 3；「已保存投递记录」显示 110 而非 100
- [x] T-007: 新增投递转化漏斗模块与 0→1 进度驱动的条形生长 / 数字滚动动画 ? Verify: 动画中途数值小于终值，终值与接口一致
- [x] T-008: 「今日重点」改为自动推导，移除对 `nextAction` 的依赖 ? Verify: `nextAction` 全空时仍列出 8 条
- [x] T-009: 实现分档排序（面试中 → 笔试中 → 准备中 → 已投递 ≥14 天 → 收藏 ≥30 天），同档内按停滞天数降序 ? Verify: 分档序号单调不降，同档内停滞天数降序
- [x] T-010: 说明文字改写为"为什么在这里"，与右侧状态标签措辞错开 ? Verify: 小字为「已 28 天没有更新笔试进展」，标签为「笔试中」，不重复
- [x] T-011: 新增「每日投递」柱状图（近 30 天，含零值空槽）与结论句 ? Verify: 30 根柱子与数据库逐日对应；结论句给出总量与距最近一次的天数
- [x] T-012: 洞察区改为两栏，右栏纵向堆叠漏斗与柱状图，底边与左栏自动对齐 ? Verify: 1920 下左右底边差 0px，且不依赖写死的图表高度
- [x] T-013: 删除「行动轨迹」「正在推进的机会」表格、「资料入口」卡片组、「最近推进」小卡片 ? Verify: 页面不再出现上述模块；侧边栏已有的入口不重复
- [x] T-014: 删除今日重点行首的装饰圆圈，行内改为两列 ? Verify: `.dashboard-check` 元素数为 0，行内 grid 只剩两列
- [x] T-015: 今日重点行点击打开投递详情弹窗而非跳转投递看板 ? Verify: 点击后 URL 仍为 `/`，弹窗打开
- [x] T-016: 弹窗复用岗位库 `job-detail__*` 版式，展示岗位信息 / 投递注意事项 / 公司介绍 ? Verify: 三个 section 齐全；事实区含投递渠道与投递入口
- [x] T-017: 弹窗状态改为可编辑下拉框，改动即调 `/status` 接口并刷新 ? Verify: 改动后数据库状态已变；产生一条 `STATUS_CHANGED` 事件（漏斗与停滞天数依赖它）
- [x] T-018: 修正弹窗头部被 `el-select` 挤压与 `job-detail__header span` 覆盖选择器内部样式的问题 ? Verify: 头部左侧 508px 不竖排；下拉框 118px；`.el-select__placeholder` 字号颜色正确
- [x] T-019: 概览区改为 grid 三列一行，移除 UI-DESIGN-SYSTEM 规定的「中等桌面重点卡整行」档位 ? Verify: 1024 / 1152 / 1280 / 1366 / 1440 / 1920 六档下均为三列一行且重点卡宽度约为行宽 1/3
- [x] T-020: 收敛样式到设计令牌（间距、圆角、字号），并把 `AgentMarkdown` 之外的遗留违规值一并清理 ? Verify: 新样式块中不出现被禁止的圆角/间距值
- [x] T-021: 只读浏览器全量验证（生产构建 + 独立端口） ? Verify: 概览 / 漏斗 / 今日重点 / 每日投递 / 弹窗 / 八档响应式全部通过，无页面错误
- [x] T-022: 新建 F-045 Spec / Plan / Tasks，并在 F-017 与 `docs/UI-DESIGN-SYSTEM.md` 标注取代关系 ? Verify: 三份文档存在；F-017 的推进机会表格验收标准已标注被取代
- [ ] T-023: 修复验证脚本改坏的三条真实投递状态与 11 条假事件 ? Verify: 状态统计回到 面试中 1 / 笔试中 3 / 已拒绝 5 / 放弃 0，今天的事件数为 0
- [ ] T-024: 在 1240px 附近实测概览卡观感，确认是否需要把单列断点从 900px 上调 ? Verify: 三张卡在 1024px 下每张约 219px，若重点卡文字过挤则上调断点
