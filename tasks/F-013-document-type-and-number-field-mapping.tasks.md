# F-013 Tasks

- [x] T-001: Define paired personal-document control behavior in feature and browser-extension contract.  
  Verify: acceptance criteria distinguish the select and text input.
- [x] T-002: Implement role-aware local mapping for shared personal-document labels.  
  Verify: text input resolves to `documentNumber`, never `documentType`.
- [x] T-003: Add regression coverage and build the extension.  
  Verify: `pnpm test` and `pnpm build` pass.
