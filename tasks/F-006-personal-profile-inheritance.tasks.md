# F-006 Tasks

- [x] T-001: 定义 PersonalProfile、继承优先级、敏感字段排除及 API 契约。  
  验证：规格、领域/API/扩展契约覆盖字段、覆盖和排除规则。
  执行记录（2026-08-11）：已建立 F-006 feature、plan 与 task，定义通用资料、档案覆盖和安全排除范围；继承顺序为通用资料 → 档案非空覆盖 → 空值，教育经历为空时整组继承。证件类型可记录，证件号码及银行卡、密码、验证码等禁止进入模型。
- [x] T-002: 添加 PersonalProfile 持久化、Zod schema、服务层与继承合成单测。  
  验证：迁移可应用；合成和拒绝敏感字段测试通过。
  执行记录（2026-08-11）：已新增 PersonalProfile 一对一模型及非破坏性迁移；新增通用资料 Zod schema、当前用户 upsert 服务与继承合成函数。`corepack pnpm db:generate`、迁移、`corepack pnpm typecheck` 和 `corepack pnpm test:profiles`（3/3）通过，覆盖非空覆盖优先、空数组继续继承、教育整组继承，以及 documentNumber/未知字段拒绝。
- [x] T-003: 实现个人资料 API，并将档案 API 的读取结果替换为合成后的安全字段。  
  验证：当前用户可读取/更新；返回不含 userId、证件号码或审计信息。
  执行记录（2026-08-11）：已提供 GET/PUT `/api/v1/personal-profile`，并让档案列表、创建和更新响应通过通用资料与档案覆盖合成。扩展继续使用原档案列表接口即可获得安全合成结果。`corepack pnpm typecheck`、继承测试（3/3）和个人资料响应最小化断言通过。
- [x] T-004: 实现“个人资料”页面与档案页继承回显/恢复继承操作。  
  验证：通用资料修改后，未覆盖档案自动回显新值；覆盖值可移除。  执行记录（2026-08-11）：已完成个人资料页、教育主记录与档案页的继承回显/恢复继承入口；修复数组字段在保存时被当作文本调用 `trim` 导致的 400/创建失败。用户已完成页面验收。
- [ ] T-005: 完成数据库、类型检查、扩展构建与手工验收记录。  
  验证：迁移、typecheck、继承测试和扩展 build 通过；用户完成资料继承验收。  执行记录（2026-08-11）：已将国家/地区、性别、微信号、政治面貌、证件类型扩展为仅限精确标签与文本控件的本地填写规则；扩展单测 6/6、继承测试 3/3、`web` typecheck 和 extension build 均通过。待用户在更新后的本地 fixture 中完成一次手工验收后勾选。
