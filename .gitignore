node_modules/
.nuxt/
.output/
dist/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store
uploads/
data/
web/server/generated/

# 本地调试产物与临时截图（不提交）
tmp-*.png
dogfood-output/
artifacts/

# 扩展构建缓存
.wxt/

# 下载的第三方 skill（依赖，非项目代码）
.agents/

# 用于对照研究的第三方浏览器扩展。上游没有 LICENSE 文件，不能随本仓库分发；
# 具体参考了哪些机制见 docs/research/upstream-provenance.md。
CampusApply-Agent-main/
CampusApply-Agent-main.zip
starjob-resume-assistant-v1.1.5/
starjob-resume-assistant-v1.1.5.zip

# 本地数据与进程文件（不提交）
*.xlsx
*.pid
backup-*.sql
# 数据库导出（如 deploy-data.sql）。不要写成 *.sql，会连 Prisma 迁移一起忽略掉。
*-data.sql
# pg_dump 的 -Fc 归档（换机迁移用），含真实账号与投递数据。
*.dump
